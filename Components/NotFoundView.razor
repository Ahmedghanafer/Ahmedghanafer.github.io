@*  Rendered by the router's NotFound branch, and by the static 404.html that
    GitHub Pages serves for deep links before the router has booted. *@

<PageTitle>Not found — Ahmed Ghanafer</PageTitle>

<section class="hero">
    <div class="shell shell--narrow">
        <span class="eyebrow">404</span>
        <h1 style="margin-block:1rem 1.25rem">Nothing here.</h1>
        <p class="lede">
            That route doesn't exist — or it did once and I moved it. Either way,
            the interesting parts are one click away.
        </p>

        <div class="hero__actions">
            <a class="btn btn--primary" href="/">
                Back to the start
                <Icon Name="arrow-right" Size="16" />
            </a>
            <a class="btn" href="@($"/projects/{ProjectCatalog.ApfSlug}")">Read the case study</a>
        </div>
    </div>
</section>
